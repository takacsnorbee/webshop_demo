import { useEffect } from 'react';
import { useNavigate } from 'react-router-dom';

function Admin():JSX.Element {
  // const navigate = useNavigate();
  // useEffect(() => {
    // if (window.localStorage.getItem('token-camp-king')
    //   || window.sessionStorage.getItem('token-camp-king')) {
    //   console.log('user lekérdezés');
    //   // todo -> kell egy api user lekérdezésre
    //   // menteni store-ba az adatait
    // } else {
    //   navigate('/');
    //   console.log('jelentkezz be');
    // }
  // }, []);

  return <div>admin</div>;
}

export default Admin;
